import './styles.css';

const ButtonIcon = () => {
    return (
        <button className="btn btn-primary btn-primary-icon">
            <p>VER CATÁLOGO</p>
        </button>
    );
}

export default ButtonIcon;